/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'umrebuldum.com'], // Add your actual image domains here
    },
};

module.exports = nextConfig;
